---
description: Clear Symfony cache (dev or prod environment)
---

# Clear Cache

// turbo-all

1. Clear the development cache:
```bash
./clear_cache.sh
```

2. If production cache is needed, run:
```bash
./clear_cache_prod.sh
```

3. Fix cache permissions if needed:
```bash
./set_cache_permissions.sh
```
