{% spaceless %}

{# "theme_datatype_form.vars.value.dataType" and "theme_datatype_form.vars.value.themeElement" get actual ODR entities... #}
{{ form_start(theme_datatype_form, {
    'method': 'POST',
    'attr': {
        'id': 'ThemeDatatypePropertiesForm_' ~ theme_datatype_form.vars.value.themeElement.id ~ '_' ~ theme_datatype_form.vars.value.dataType.id,
        'class': 'ODRThemeDatatypePropertiesForm pure-form pure-form-aligned'
    }
}) }}

    <fieldset>
        <div class="pure-control-group">
            <label for="{{ theme_datatype_form.display_type.vars.id }}">
                <strong>{{ theme_datatype_form.display_type.vars.label }}</strong>
            </label>
            {{ form_errors(theme_datatype_form.display_type) }}
            {{ form_widget(theme_datatype_form.display_type, { 'attr': {'style': 'width:auto'} }) }}
        </div>

        {{ form_rest(theme_datatype_form) }}
    </fieldset>

{{ form_end(theme_datatype_form) }}

{% endspaceless %}
