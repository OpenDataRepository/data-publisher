{% apply spaceless %}

<div class="ODRTagWrapper_view ODRTagWrapper">
    <ul class="ODRTagGroup">
        {% for tag_id, tag in stacked_tags %}
            {% include '@ODRAdmin/MassEdit/tag.html.twig' with {
                'tag': tag,
            } %}
        {% endfor %}
    </ul>
</div>

{% endapply %}
