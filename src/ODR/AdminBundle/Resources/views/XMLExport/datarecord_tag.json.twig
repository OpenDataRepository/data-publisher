{% set tag_meta = tag.tagMeta %}

{
    "id": {{ tag.id }},
    "template_tag_uuid": {{ tag.tagUuid|json_encode|raw }},
    {% if tag.userCreated %}
        "user_created": "1",
    {% endif %}
    "name": "{{ tag.tagName }}",
    "updated_at": "{{ tag.created|date('Y-m-d H:i:s') }}",
    "selected": 1
},
