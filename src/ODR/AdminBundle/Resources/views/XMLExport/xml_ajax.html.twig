{% spaceless %}

{% set datatype = datatype_array[initial_datatype_id] %}

<?xml version="1.0"?>
<datarecords>
    <datarecord datatype_name="{{ datatype.dataTypeMeta.xml_shortName }}">
        {% set is_top_level = 1 %}
        {% set is_link = 0 %}
        {% import "ODRAdminBundle:XMLExport:xml_childtype.html.twig" as mychildform %}
        {{ mychildform.input(datatype_array, datarecord_array, theme_id, initial_datatype_id, initial_datarecord_id, is_top_level, is_link, using_metadata, baseurl) }}
    </datarecord>
</datarecords>

{% endspaceless %}
