{% spaceless %}

{% set datatype = datatype_array[initial_datatype_id] %}
{% set is_top_level = 1 %}
{% set is_link = 0 %}

<?xml version="1.0" encoding="UTF-8"?>

    {# using macro on purpose #}
    {% import "ODRAdminBundle:XMLExport:datatype_childtype.xml.twig" as mychildform %}
    {{ mychildform.input(datatype_array, theme_id, initial_datatype_id, is_top_level, is_link, using_metadata, baseurl, version) }}

{% endspaceless %}
