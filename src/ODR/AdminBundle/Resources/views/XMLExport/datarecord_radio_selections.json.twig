{% for radio_selection in radio_selections %}
    {% set radio_option = radio_selection.radioOption %}
        {% if radio_option is not null and radio_selection.selected %}
        {
            "id": {{ radio_option.id }},
            "template_radio_option_uuid": {{ radio_option.radioOptionUuid|json_encode|raw }},
            "name": "{{ radio_option.optionName }}",
            {% if radio_option.userCreated %}
                "user_created": "1",
            {% endif %}
            "option_created": "{{ radio_option.created|date('Y-m-d H:i:s') }}",
            "option_selected": "{{ radio_selection.created|date('Y-m-d H:i:s') }}",
            "selected": 1
        },
    {% endif %}
{% endfor %}