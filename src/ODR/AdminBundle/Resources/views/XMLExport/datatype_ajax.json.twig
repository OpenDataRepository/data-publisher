{% set datatype = datatype_array[initial_datatype_id] %}

{% set is_top_level = 1 %}
{% set is_link = 0 %}

{
    {# using macro on purpose #}
    {% import "ODRAdminBundle:XMLExport:datatype_childtype.json.twig" as mychildform %}
    {{ mychildform.input(datatype_array, theme_array, initial_datatype_id, is_top_level, is_link, using_metadata, baseurl, version) }}
}
