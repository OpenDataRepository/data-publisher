{% macro input(datatype_array, target_datatype_id, theme_id, is_top_level) %}

{% spaceless %}
    {% include 'ODRAdminBundle:XSDCreate:xsd_fieldarea.html.twig' with {
        'datatype_array': datatype_array,
        'target_datatype_id': target_datatype_id,
        'theme_id': theme_id,

        'is_top_level': is_top_level,
    } %}
{% endspaceless %}

{% endmacro %}
