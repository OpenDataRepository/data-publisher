{#{% extends 'HWIOAuthBundle::layout.html.twig' %}#}
{#
{% block hwi_oauth_content %}
    {% if error is defined and error %}
        <span>{{ error }}</span>
    {% endif %}
#}
    {% for owner in hwi_oauth_resource_owners() %}
        <span class="odr_oauth_{{ owner }}" style="width: 200px; padding: 10px; margin: 10px; background: #d33333;">
            <a href="{{ hwi_oauth_login_url(owner) }}">Log in using {{ owner }}</a>
        </span>
    {% endfor %}
{#
{% endblock hwi_oauth_content %}
#}
