odr_plugins.base.csvtable:
    name: CSV Table Plugin
    category: Base
    datatype: false
    render: true
    version: 1.1.0
    override_fields: false
    override_child: false
    description: CSV files can be displayed in the page in a spreadsheet format using this plugin.
    registered_events:
    required_fields:
        csv_file:
            name: CSV File
            description: A file field containing CSV-formatted data.
            type: File
    config_options:
