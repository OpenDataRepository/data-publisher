{% spaceless %}

    {% set datafield_id = datafield.id %}
    {% set fieldname = datafield['dataFieldMeta']['fieldName'] %}
    {% set description = datafield['dataFieldMeta']['description'] %}

    <span class="pure-u-1">
        <label for="datafield_{{ datafield_id }}" class="pure-u-1">
            <span title="{{ description }}">{{ fieldname }}:</span>

            <span class="ODRFileInfoDiv">
                <i class="fa fa-lg fa-info-circle"></i>
                <span class="ODRFileInfo">
                    <p>This field is searched differently than the rest of ODR, because the contents of this field are supposed to be Chemical Elements.</p>
                    <p>The search results will have ALL OF the elements entered without a prefix.</p>
                    <p>The results will have AT LEAST ONE OF the elements prefixed with the "~" character.</p>
                    <p>The results will have NONE OF the elements prefixed with the "!" character.</p>
                    <p>As an example, the string "C O ~N ~H !Na !Ca" will return results that have both C and O, and have either N or H, and have neither Na nor Ca.</p>
                    <p>Additionally, the term "all" is a shortcut to place every unspecified element in the NONE OF category.</p>
                    <p>As such, the string "C all" will only return results that have C, and no other element.</p>
                </span>
            </span>

        </label>
    </span>

    <input type="text" class="ODRInput pure-u-1" id="datafield_{{ datafield_id }}" name="{{ datafield_id }}" rel="{{ datatype_id }}" {% if preset_value != '' %}value="{{ preset_value }}"{% endif %}/>

{% endspaceless %}
