imports:
    - { resource: "../../Plugins/Base/config.yml" }
    - { resource: "../../Plugins/Chemin/config.yml" }
    - { resource: "../../Plugins/AHED/config.yml" }
    - { resource: "../../Plugins/Metadata/config.yml" }

parameters:
    # This should point to the base directory for the render plugin yml and php files...used to
    #  determine which plugins are on the server
    odr_plugin_basedir: "%kernel.root_dir%/../src/ODR/OpenRepository/GraphBundle/Plugins"

    # This should NOT have a trailing backslash...also shouldn't be quoted apparently
    odr_plugin_base_classpath: ODR\OpenRepository\GraphBundle\Plugins
